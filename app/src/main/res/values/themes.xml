<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.ChatApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ui_blue</item>
        <item name="colorPrimaryVariant">@color/ui_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ui_blue</item>
        <item name="colorSecondaryVariant">@color/ui_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">false</item>
        <!-- Ensure no title bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Fullscreen theme for image viewing -->
    <style name="Theme.ChatApp.Fullscreen" parent="Theme.ChatApp">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash theme used by SplashActivity -->
    <style name="Theme.ChatApp.Splash" parent="Theme.ChatApp">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="TransparentDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- Calendar dark dialog theme (TTW Schedule theme) -->
    <style name="CalendarDarkDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="colorPrimary">@color/cal_accent</item>
        <item name="colorOnSurface">@color/cal_text_primary</item>
        <item name="android:background">@color/cal_surface</item>
        <item name="buttonBarPositiveButtonStyle">@style/CalendarDarkDialog.Button</item>
        <item name="buttonBarNegativeButtonStyle">@style/CalendarDarkDialog.Button.Negative</item>
        <item name="buttonBarNeutralButtonStyle">@style/CalendarDarkDialog.Button.Negative</item>
        <item name="android:textColor">@color/cal_text_primary</item>
        <item name="android:textColorPrimary">@color/cal_text_primary</item>
        <item name="android:textColorSecondary">@color/cal_text_secondary</item>
    </style>

    <style name="CalendarDarkDialog.Button" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/cal_accent</item>
    </style>

    <style name="CalendarDarkDialog.Button.Negative" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/cal_text_secondary</item>
    </style>

</resources>