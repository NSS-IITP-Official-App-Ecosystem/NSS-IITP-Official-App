<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Calendar launcher activity for testing -->
        <activity
            android:name=".CalendarLauncherActivity"
            android:exported="true">
            <!-- Enable this intent filter only when testing the module independently -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        
        <!-- Calendar module activity -->
        <activity
            android:name=".CalendarActivity"
            android:exported="false" />
            
        <!-- Any other calendar-specific activities go here -->
    </application>

</manifest> 